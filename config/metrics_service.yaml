apiVersion: v1
kind: Service
metadata:
  name: addon-operator-metrics
  namespace: addon-operator
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: addon-operator
