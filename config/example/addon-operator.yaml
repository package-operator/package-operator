apiVersion: addons.managed.openshift.io/v1alpha1
kind: AddonOperator
metadata:
  name: addon-operator
spec:
  ocm:
    endpoint: http://ocm-api-mock.ocm-api-mock.svc.cluster.local
    secret:
      name: ocm-api-mock
      namespace: ocm-api-mock
